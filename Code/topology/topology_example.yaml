# Example topology config for topology_auto.py
# Use: python topology_auto.py --config topology_example.yaml
#
# Only Field zone is scalable. Control zone = 1 host, IT zone = 2 hosts (fixed).
field: 2
bandwidth: 5
